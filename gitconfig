# .gitconfig for regular users

[user]
	name = Benoît Merlet
[color]
	ui = true
[core]
	autocrlf = false
	editor = vim
	excludesfile = ~/.gitglobalignore
[alias]
	log-branch = log --oneline --decorate=false master..HEAD
	changed-files = "!f() { m=$(git merge-base HEAD master); git diff --name-only HEAD $m; }; f"
[log]
	decorate = true
[pull]
	rebase = true
[status]
	showUntrackedFiles = all
[git-up "fetch"]
	all = true
[rebase]
	autosquash = true
